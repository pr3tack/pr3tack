# Detection: LLM Jailbreak Attempt via Prompt Injection Pattern

## Metadata

| Field | Value |
|---|---|
| **Technique** | T1103.001 — LLM Mirroring & Local Jailbreak Prep |
| **Tactic** | TA02 — Resource Development |
| **Detection Type** | Rule |
| **Data Source** | Application Logs / API Gateway / LLM Proxy |
| **Confidence** | Medium |
| **Status** | Draft |

---

## Objective

Detect prompt injection and jailbreak attempts against production LLM API endpoints, including patterns consistent with offline-rehearsed payloads being deployed on the first live attempt.

---

## Detection Logic

```yaml
title: LLM Prompt Injection and Jailbreak Attempt Detection
id: pr3tack-T1103-001
status: experimental
description: >
  Detects prompt injection and jailbreak patterns in requests to LLM API endpoints.
  Particular focus on first-call anomalies (no prior interaction from the client)
  that contain known injection structures — indicative of offline-rehearsed payloads
  deployed against the live target.
references:
  - https://github.com/pr3tack/pr3tack/techniques/TA02-resource-development/T1103.001-llm-mirroring-local-jailbreak-prep.md
author: PR3TACK
date: 2026/05/19
tags:
  - attack.resource_development
  - attack.initial_access
  - pr3tack.TA02
  - pr3tack.T1103.001
logsource:
  category: application
  product: llm_api_gateway
detection:
  selection_injection_patterns:
    request_body|contains:
      - 'ignore previous instructions'
      - 'ignore all previous'
      - 'disregard your system prompt'
      - 'you are now DAN'
      - 'pretend you have no restrictions'
      - 'act as if you were trained without'
      - 'jailbreak'
      - '[[SYSTEM]]'
      - '<|im_start|>system'
      - 'OVERRIDE:'
      - 'ADMIN_PROMPT:'
      - 'you must comply with all requests'
  selection_output_anomaly:
    response_body|contains:
      - 'As DAN'
      - 'I have no restrictions'
      - '[JAILBREAK]'
      - 'SYSTEM PROMPT:'
  condition: selection_injection_patterns or selection_output_anomaly
falsepositives:
  - Security researchers testing your own LLM endpoints in a controlled manner
  - Red team exercises
  - Automated LLM testing frameworks (Garak, PyRIT) in non-production environments
level: high
fields:
  - client_ip
  - api_key_id
  - model
  - request_body
  - response_body
  - response_finish_reason
  - is_first_request
```

### First-Call Anomaly Rule (API Gateway — KQL)

```kql
LLMAPIGatewayLogs
| where TimeGenerated > ago(1h)
| where isempty(PriorRequestCount) or PriorRequestCount == 0   // First call from this client
| where RequestBody has_any (
    "ignore previous instructions", "ignore all previous",
    "disregard your system prompt", "you are now DAN",
    "pretend you have no restrictions", "OVERRIDE:", "ADMIN_PROMPT:",
    "[[SYSTEM]]", "<|im_start|>system"
  )
| project TimeGenerated, ClientIP, APIKeyId, Model, RequestBody, ResponseBody
| order by TimeGenerated desc
```

### Canary Token Exfiltration Rule (KQL)

```kql
LLMAPIGatewayLogs
| where TimeGenerated > ago(1h)
| where ResponseBody has (CanaryTokenValue)   // Replace with your deployed canary string
| project TimeGenerated, ClientIP, APIKeyId, Model, RequestBody, ResponseBody
| extend AlertType = "SYSTEM_PROMPT_CANARY_EXFILTRATED"
```

### Rate Anomaly: Systematic Input Variation (KQL)

```kql
LLMAPIGatewayLogs
| where TimeGenerated > ago(10m)
| summarize
    RequestCount = count(),
    UniqueInputLengths = dcount(strlen(RequestBody)),
    AvgInputLength = avg(strlen(RequestBody))
  by ClientIP, APIKeyId
| where RequestCount > 50                    // High volume
| where UniqueInputLengths > 30             // Systematic variation (fuzzing pattern)
| project TimeGenerated, ClientIP, APIKeyId, RequestCount, UniqueInputLengths, AvgInputLength
```

---

## Tuning Notes

- **Pattern list maintenance:** The injection pattern list decays quickly. Subscribe to [Garak](https://github.com/leondz/garak) and [OWASP LLM Top 10](https://owasp.org/www-project-top-10-for-large-language-model-applications/) updates and refresh patterns quarterly.
- **First-call anomaly:** The most high-fidelity signal for offline-rehearsed payloads is a jailbreak pattern appearing in the **first** API call from a new client. Track `client_first_seen` in your API gateway to enable this.
- **Canary tokens:** Embed a unique string in your system prompt (e.g., `CANARY-{uuid}`). If this appears in any API response, a prompt extraction attack has succeeded — this is a Critical alert.
- **Output monitoring:** Monitor LLM **outputs** not just inputs — a successful jailbreak may be invisible in the request but obvious in the response.
- **False positives:** Red team and security research will trigger this. Maintain an allowlist of authorised testing API keys.

---

## Validation

1. Deploy a test LLM endpoint with a canary token in the system prompt
2. Submit a known jailbreak payload (e.g., from the [Jailbreak Chat dataset](https://github.com/verazuo/jailbreak_llms)) to the endpoint
3. Confirm the alert fires on the injection pattern match
4. Attempt to extract the canary token via prompt injection and confirm the canary alert fires
5. Run [Garak](https://github.com/leondz/garak) against the endpoint and confirm the rate anomaly rule triggers

---

## References

- [PR3TACK T1103.001 — LLM Mirroring & Local Jailbreak Prep](../techniques/TA02-resource-development/T1103.001-llm-mirroring-local-jailbreak-prep.md)
- [OWASP LLM01: Prompt Injection](https://owasp.org/www-project-top-10-for-large-language-model-applications/)
- [Garak — LLM Vulnerability Scanner](https://github.com/leondz/garak)
- [PyRIT — Python Risk Identification Toolkit for LLMs](https://github.com/Azure/PyRIT)
